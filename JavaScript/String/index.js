// 1. Prompt the user to enter their ful lname. Generate the username for them based on the input. Start username with @, followed by their full name and eneding with the fullname length.


// let fullName= prompt("Enter your fullname without space");
// let username= "@" + fullName + fullName.length;

// console.log(`@${fullName.toLocaleLowerCase()}${fullName.length}`)

// console.log(username);



